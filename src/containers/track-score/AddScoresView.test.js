/* global it */
import 'react-native';
// import React from 'react';
// import renderer from 'react-test-renderer';

it('AddScoresView renders correctly for two players', () => {
  //  TODO: We'll need to simplify the component to test it properly.
  // const tree = renderer.create(
  // <AddScoresView />,
  // ).toJSON();

  // expect(tree).toMatchSnapshot();
});
